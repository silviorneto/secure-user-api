info:
  name: Get Profile
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/users/profile"
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
